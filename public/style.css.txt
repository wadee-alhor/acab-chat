<!doctype html>
<html lang="ar" dir="rtl" class="h-full">
 <head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>رمضان كريم</title>
  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="/_sdk/element_sdk.js"></script>
  <link href="https://fonts.googleapis.com/css2?family=Amiri:wght@400;700&amp;family=Tajawal:wght@400;500;700&amp;display=swap" rel="stylesheet">
  <style>
    html, body { height: 100%; width: 100%; margin: 0; padding: 0; overflow-x: hidden; }
    * { font-family: 'Tajawal', sans-serif; }
    .amiri { font-family: 'Amiri', serif; }
    
    @keyframes float { 0%, 100% { transform: translateY(0px); } 50% { transform: translateY(-15px); } }
    @keyframes glow { 0%, 100% { box-shadow: 0 0 20px rgba(255, 215, 0, 0.3), inset 0 0 20px rgba(255, 215, 0, 0.1); } 50% { box-shadow: 0 0 40px rgba(255, 215, 0, 0.6), inset 0 0 30px rgba(255, 215, 0, 0.2); } }
    @keyframes shimmer { 0% { background-position: -1000px 0; } 100% { background-position: 1000px 0; } }
    @keyframes fadeInUp { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
    @keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.6; } }
    
    .glow-border { animation: glow 3s ease-in-out infinite; }
    .float-element { animation: float 6s ease-in-out infinite; }
    .card-entrance { animation: fadeInUp 0.6s ease-out forwards; }
    .pulse-icon { animation: pulse 2s ease-in-out infinite; }
    
    body { 
      background: linear-gradient(135deg, #0f0c29 0%, #1a1844 50%, #16213e 100%);
      position: relative;
      overflow-y: auto;
    }
    
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: 
        radial-gradient(circle at 20% 50%, rgba(255, 215, 0, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 80% 80%, rgba(255, 165, 0, 0.1) 0%, transparent 50%);
      pointer-events: none;
      z-index: 0;
    }
    
    .content-wrapper { position: relative; z-index: 1; }
    
    .section-toggle { cursor: pointer; transition: all 0.3s ease; }
    .section-toggle.active { transform: scale(1.08); }
    .section-toggle:hover { transform: translateY(-5px); }
    
    .modal-content { animation: fadeInUp 0.4s ease-out; }
    
    .starfield {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: 0;
    }
    
    .star {
      position: absolute;
      width: 2px;
      height: 2px;
      background: white;
      border-radius: 50%;
      opacity: 0.3;
      animation: twinkle 3s infinite;
    }
    
    @keyframes twinkle { 0%, 100% { opacity: 0.2; } 50% { opacity: 0.8; } }
  </style>
  <style>body { box-sizing: border-box; }</style>
  <script src="/_sdk/data_sdk.js" type="text/javascript"></script>
 </head>
 <body class="h-full">
  <div class="starfield" id="starfield"></div>
  <div class="content-wrapper min-h-full pb-8">
   <!-- Header -->
   <header class="sticky top-0 z-30 bg-gradient-to-r from-[#0f0c29]/95 via-[#1a1844]/95 to-[#16213e]/95 backdrop-blur-lg border-b-2 border-yellow-500/40 px-4 py-6">
    <div class="max-w-6xl mx-auto">
     <div class="flex items-center justify-between mb-4">
      <div>
       <h1 id="app-title" class="text-4xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-yellow-300 via-orange-300 to-yellow-200 mb-1">رمضان كريم</h1>
       <p class="text-yellow-200/60 text-sm flex items-center gap-2">🌙 مساعدك الروحي في شهر الخير</p>
      </div><button id="settings-btn" class="glow-border bg-gradient-to-r from-yellow-500/20 to-orange-500/20 hover:from-yellow-500/40 hover:to-orange-500/40 border border-yellow-500/60 rounded-xl px-5 py-3 flex items-center gap-2 transition-all font-bold text-yellow-300"> <span class="text-xl">⚙️</span> إعدادات </button>
     </div>
    </div>
   </header><!-- Main Content -->
   <div class="max-w-6xl mx-auto px-4 py-8">
    <!-- Featured Next Prayer -->
    <div class="mb-8 card-entrance" style="animation-delay: 0.1s">
     <div class="glow-border bg-gradient-to-br from-yellow-600/30 via-orange-600/20 to-red-600/30 backdrop-blur-md rounded-3xl p-8 border-2 border-yellow-500/50 float-element">
      <div class="text-center space-y-4">
       <p class="text-yellow-200/70 text-lg font-semibold">⏰ الصلاة القادمة</p>
       <h2 id="next-prayer-name" class="amiri text-6xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-yellow-300 to-orange-300">المغرب</h2>
       <div class="flex justify-center gap-4 py-6">
        <div class="bg-black/50 rounded-2xl px-4 py-3 border border-yellow-500/30">
         <span id="countdown-hours" class="text-4xl font-bold text-yellow-300 block">00</span>
         <p class="text-xs text-yellow-200/60 mt-1">ساعة</p>
        </div>
        <div class="text-4xl text-yellow-400 self-center font-bold">
         :
        </div>
        <div class="bg-black/50 rounded-2xl px-4 py-3 border border-yellow-500/30">
         <span id="countdown-minutes" class="text-4xl font-bold text-yellow-300 block">00</span>
         <p class="text-xs text-yellow-200/60 mt-1">دقيقة</p>
        </div>
        <div class="text-4xl text-yellow-400 self-center font-bold">
         :
        </div>
        <div class="bg-black/50 rounded-2xl px-4 py-3 border border-yellow-500/30">
         <span id="countdown-seconds" class="text-4xl font-bold text-yellow-300 block">00</span>
         <p class="text-xs text-yellow-200/60 mt-1">ثانية</p>
        </div>
       </div>
       <p class="text-yellow-200/80 text-lg">الموعد: <span id="next-prayer-time" class="text-yellow-300 font-bold text-2xl">--:--</span></p>
      </div>
     </div>
    </div><!-- Prayer Times Grid -->
    <div class="mb-8 card-entrance" style="animation-delay: 0.2s">
     <h3 class="text-3xl font-bold text-yellow-300 mb-5 flex items-center gap-2">🕌 مواقيت الصلاة اليوم</h3>
     <div id="prayer-times-grid" class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-4"></div>
    </div><!-- Iftar & Suhoor -->
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mb-8 card-entrance" style="animation-delay: 0.3s">
     <div class="bg-gradient-to-br from-orange-600/30 to-red-600/20 backdrop-blur-md rounded-3xl p-6 border-2 border-orange-500/40 hover:border-orange-500/70 transition">
      <div class="flex items-center gap-3 mb-4">
       <span class="text-5xl pulse-icon">🌅</span>
       <div>
        <h4 class="text-orange-300 font-bold text-lg">وقت الإفطار</h4>
        <p class="text-orange-200/60 text-sm">أذان المغرب</p>
       </div>
      </div>
      <p id="iftar-time" class="amiri text-4xl text-center text-white font-bold">--:--</p>
     </div>
     <div class="bg-gradient-to-br from-blue-600/30 to-purple-600/20 backdrop-blur-md rounded-3xl p-6 border-2 border-blue-500/40 hover:border-blue-500/70 transition">
      <div class="flex items-center gap-3 mb-4">
       <span class="text-5xl pulse-icon">🌙</span>
       <div>
        <h4 class="text-blue-300 font-bold text-lg">وقت السحور</h4>
        <p class="text-blue-200/60 text-sm">أذان الفجر</p>
       </div>
      </div>
      <p id="suhoor-time" class="amiri text-4xl text-center text-white font-bold">--:--</p>
     </div>
    </div><!-- Sections Grid -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 card-entrance" style="animation-delay: 0.4s">
     <div class="section-toggle active" onclick="switchSection('prayer-section', this)">
      <div class="bg-gradient-to-br from-yellow-600/40 to-orange-600/30 hover:from-yellow-600/60 hover:to-orange-600/50 backdrop-blur-md rounded-2xl p-6 border-2 border-yellow-500/40 hover:border-yellow-500/80 transition h-32 flex flex-col items-center justify-center cursor-pointer">
       <span class="text-5xl mb-2 block">🕌</span>
       <h3 class="text-yellow-300 font-bold text-lg text-center">مواقيت الصلاة</h3>
      </div>
     </div>
     <div class="section-toggle" onclick="switchSection('quran-section', this)">
      <div class="bg-gradient-to-br from-green-600/40 to-emerald-600/30 hover:from-green-600/60 hover:to-emerald-600/50 backdrop-blur-md rounded-2xl p-6 border-2 border-green-500/40 hover:border-green-500/80 transition h-32 flex flex-col items-center justify-center cursor-pointer">
       <span class="text-5xl mb-2 block">🤲</span>
       <h3 class="text-green-300 font-bold text-lg text-center">قائمة التسبيح</h3>
      </div>
     </div>
     <div class="section-toggle" onclick="switchSection('dua-section', this)">
      <div class="bg-gradient-to-br from-purple-600/40 to-pink-600/30 hover:from-purple-600/60 hover:to-pink-600/50 backdrop-blur-md rounded-2xl p-6 border-2 border-purple-500/40 hover:border-purple-500/80 transition h-32 flex flex-col items-center justify-center cursor-pointer">
       <span class="text-5xl mb-2 block">🤲</span>
       <h3 class="text-purple-300 font-bold text-lg text-center">أدعية رمضان</h3>
      </div>
     </div>
     <div class="section-toggle" onclick="switchSection('tips-section', this)">
      <div class="bg-gradient-to-br from-blue-600/40 to-cyan-600/30 hover:from-blue-600/60 hover:to-cyan-600/50 backdrop-blur-md rounded-2xl p-6 border-2 border-blue-500/40 hover:border-blue-500/80 transition h-32 flex flex-col items-center justify-center cursor-pointer">
       <span class="text-5xl mb-2 block">💡</span>
       <h3 class="text-blue-300 font-bold text-lg text-center">نصائح رمضان</h3>
      </div>
     </div>
    </div><!-- Sections Content -->
    <div id="sections-content" class="mt-8"></div>
   </div>
  </div>
  <script>
    const defaultConfig = {
      app_title: 'رمضان كريم',
      city_label: 'مكة المكرمة'
    };

    // Duas data
    const duas = [
      { 
        title: '🤲 دعاء الفطر الكامل', 
        content: 'اللهم لك صمت وبك آمنت وعليك توكلت وعلى رزقك أفطرت، اللهم تقبل مني إنك أنت السميع العليم. اللهم إني أسألك برحمتك التي وسعت كل شيء أن تغفر لي ذنوبي أجمعين',
        details: 'يستحب الدعاء عند الإفطار قبل تناول الطعام والشراب'
      },
      { 
        title: '🌙 دعاء السحور', 
        content: 'اللهم إني نويت صوم غد من شهر رمضان المبارك، فتقبل مني وأعنّي عليه. اللهم إني أسألك في هذا السحر رحمة منك تغني بها عني المسألة من أحد من خلقك',
        details: 'أفضل وقت للسحور هو الثلث الأخير من الليل'
      },
      { 
        title: '🙏 دعاء التراويح', 
        content: 'اللهم اجعل لنا في هذا الشهر نصيباً من رحمتك وفضلك وتوفيقك. اللهم لا تحرمنا خير ما في هذا الشهر، واجعل الخير في كل ليلة من لياليه',
        details: 'دعاء مستحب في كل ليلة من ليالي رمضان'
      },
      { 
        title: '💫 دعاء ليلة القدر', 
        content: 'اللهم إنك عفو تحب العفو فاعفُ عني. اللهم إن ليلة القدر خير من ألف شهر، فإذا أدركتها فاجعل فيها رحمة وغفرانا وعتقا من النار',
        details: 'تُطلب هذه الليلة في العشر الأواخر من رمضان وخاصة الليالي الفردية'
      },
      { 
        title: '❤️ دعاء العفو والمغفرة', 
        content: 'رب اغفر لي وارحمني وعافِني وارزقني وحسّن عاقبتي في الدنيا والآخرة. اللهم اغفر لي ما قدمت وما أخرت وما أسررت وما أعلنت أنت إلهي لا إله إلا أنت',
        details: 'من أجمل الأدعية التي تجمع بين طلب العفو والغفران'
      },
      {
        title: '🌟 دعاء الاستغفار', 
        content: 'استغفر الله العظيم الذي لا إله إلا هو الحي القيوم وأتوب إليه. اللهم اغفر لي ولوالديّ ولجميع المسلمين برحمتك يا أرحم الراحمين',
        details: 'من أعظم الأدعية التي يستحب الإكثار منها في رمضان'
      },
      {
        title: '🤝 دعاء لقضاء الحوائج',
        content: 'اللهم يا رب اسمع دعائي وأجبني على ما دعوتك عليه. اللهم لا تحرمني خيرك ولا تردّ دعائي ويسّر لي كل عسير وارزقني من حيث لا أحتسب',
        details: 'من الأدعية المستجابة خاصة في ساعات الإجابة'
      },
      {
        title: '✨ دعاء الحمد والشكر',
        content: 'الحمد لله الذي أعطاني من فضله ما يكفيني ويزيدني. الحمد لله على كل حال والشكر لله على نعمه التي لا تحصى. اللهم لك الحمد كما ينبغي لجلال وجهك وعظيم سلطانك',
        details: 'من أجمل الأدعية التي تعبّر عن الامتنان لله تعالى'
      }
    ];

    // سُبحات التسبيح
    const tasbeehs = [
      { text: 'سُبحان الله', count: 33, icon: '✨' },
      { text: 'الحمد لله', count: 33, icon: '💛' },
      { text: 'لا إله إلا الله', count: 33, icon: '🤲' },
      { text: 'الله أكبر', count: 34, icon: '📢' }
    ];

    // Ramadan tips
    const tips = [
      { icon: '💧', title: 'شرب الماء', desc: 'اشرب الماء بكثرة بين الإفطار والسحور' },
      { icon: '🥗', title: 'غذاء صحي', desc: 'تناول تمر وحليب وأطعمة خفيفة' },
      { icon: '📖', title: 'قراءة القرآن', desc: 'ختم القرآن مرة على الأقل في الشهر' },
      { icon: '🕌', title: 'الصلاة المفروضة', desc: 'لا تفوت صلاة واحدة مع الجماعة' },
      { icon: '⏰', title: 'النوم الكافي', desc: 'احصل على 6-8 ساعات نوم متواصلة' },
      { icon: '🤝', title: 'التطوع', desc: 'ساعد المحتاجين والفقراء' }
    ];

    const prayers = [
      { id: 'fajr', name: 'الفجر', icon: '🌄' },
      { id: 'sunrise', name: 'الشروق', icon: '🌅' },
      { id: 'dhuhr', name: 'الظهر', icon: '☀️' },
      { id: 'asr', name: 'العصر', icon: '🌤️' },
      { id: 'maghrib', name: 'المغرب', icon: '🌇' },
      { id: 'isha', name: 'العشاء', icon: '🌙' }
    ];

    let prayerTimes = {};
    let currentLocation = '21.4225,39.8262';
    let userLocation = null;

    // Create starfield
    function createStarfield() {
      const starfield = document.getElementById('starfield');
      for (let i = 0; i < 30; i++) {
        const star = document.createElement('div');
        star.className = 'star';
        star.style.left = Math.random() * 100 + '%';
        star.style.top = Math.random() * 100 + '%';
        star.style.animationDelay = Math.random() * 3 + 's';
        starfield.appendChild(star);
      }
    }

    // Initialize Element SDK
    if (window.elementSdk) {
      window.elementSdk.init({
        defaultConfig,
        onConfigChange: async (config) => {
          document.getElementById('app-title').textContent = config.app_title || defaultConfig.app_title;
        },
        mapToCapabilities: (config) => ({
          recolorables: [],
          borderables: [],
          fontEditable: undefined,
          fontSizeable: undefined
        }),
        mapToEditPanelValues: (config) => new Map([
          ['app_title', config.app_title || defaultConfig.app_title],
          ['default_country', config.default_country || defaultConfig.default_country],
          ['azan_sound', config.azan_sound || defaultConfig.azan_sound]
        ])
      });
    }

    // Settings button
    document.getElementById('settings-btn').addEventListener('click', () => {
      openSettingsFullPage();
      playSound('soft');
    });

    function openSettingsFullPage() {
      const modal = document.createElement('div');
      modal.className = 'fixed inset-0 bg-black/95 backdrop-blur z-50 overflow-y-auto';
      modal.innerHTML = `
        <div class="max-w-4xl mx-auto px-4 py-8">
          <button onclick="this.closest('.fixed').remove(); playSound('soft');" class="fixed top-4 right-4 bg-gradient-to-r from-red-600 to-red-700 hover:from-red-700 hover:to-red-800 text-white font-bold px-6 py-3 rounded-full text-3xl z-60 transition transform hover:scale-110 leading-none">←</button>
          <div class="mt-20 mb-8">
            <h1 class="text-5xl font-bold text-yellow-300 text-center mb-2">⚙️ الإعدادات الكاملة</h1>
            <p class="text-yellow-200/60 text-center">قم بتخصيص التطبيق حسب احتياجاتك</p>
          </div>
          
          <div class="space-y-6">
            <!-- المدينة والدولة -->
            <div class="bg-gradient-to-br from-blue-600/40 to-blue-600/20 backdrop-blur-md rounded-3xl p-8 border-2 border-blue-500/40 card-entrance">
              <div class="flex items-center gap-3 mb-6">
                <span class="text-4xl">🗺️</span>
                <h2 class="text-2xl font-bold text-blue-300">المدينة والدولة</h2>
              </div>
              <select id="city-select-fullpage" class="w-full bg-white/10 border border-blue-500/40 rounded-xl px-4 py-4 text-white font-semibold focus:outline-none focus:border-blue-500/80 transition text-lg mb-4">
                <option value="21.4225,39.8262">🇸🇦 مكة المكرمة - السعودية</option>
                <option value="24.7136,46.6753">🇸🇦 الرياض - السعودية</option>
                <option value="21.5433,39.1728">🇸🇦 جدة - السعودية</option>
                <option value="26.1551,50.1857">🇸🇦 الدمام - السعودية</option>
                <option value="27.5373,41.7587">🇸🇦 المدينة المنورة - السعودية</option>
                <option value="26.3351,50.2055">🇸🇦 الخبر - السعودية</option>
                <option value="30.0444,31.2357">🇪🇬 القاهرة - مصر</option>
                <option value="31.2001,29.9187">🇪🇬 الإسكندرية - مصر</option>
                <option value="27.1767,33.8345">🇪🇬 الأقصر - مصر</option>
                <option value="31.9454,35.9284">🇯🇴 عمّان - الأردن</option>
                <option value="32.1345,35.7662">🇵🇸 رام الله - فلسطين</option>
                <option value="33.5138,36.2765">🇸🇾 دمشق - سوريا</option>
                <option value="33.5106,36.2765">🇱🇧 بيروت - لبنان</option>
                <option value="24.4539,54.3773">🇦🇪 أبو ظبي - الإمارات</option>
                <option value="25.2048,55.2708">🇦🇪 دبي - الإمارات</option>
                <option value="29.3759,47.9774">🇰🇼 الكويت</option>
                <option value="26.1445,50.5577">🇧🇭 المنامة - البحرين</option>
                <option value="25.3548,51.1839">🇶🇦 الدوحة - قطر</option>
              </select>
              <p class="text-blue-200/70 text-sm">✓ سيتم تحديث أوقات الصلاة تلقائياً عند التغيير</p>
            </div>

            <!-- صوت الأذان -->
            <div class="bg-gradient-to-br from-green-600/40 to-green-600/20 backdrop-blur-md rounded-3xl p-8 border-2 border-green-500/40 card-entrance" style="animation-delay: 0.1s">
              <div class="flex items-center gap-3 mb-6">
                <span class="text-4xl">🔔</span>
                <h2 class="text-2xl font-bold text-green-300">صوت الأذان</h2>
              </div>
              <select id="azan-sound-fullpage" class="w-full bg-white/10 border border-green-500/40 rounded-xl px-4 py-4 text-white font-semibold focus:outline-none focus:border-green-500/80 transition text-lg mb-4">
                <option value="default">📢 الأذان التقليدي الكلاسيكي</option>
                <option value="peaceful">🎵 أذان هادئ وجميل</option>
                <option value="classic">🎼 أذان كلاسيكي عريق</option>
                <option value="modern">🎙️ أذان حديث واضح</option>
              </select>
              <button onclick="testAzanSound()" class="w-full bg-gradient-to-r from-green-600/80 to-emerald-600/80 hover:from-green-600 hover:to-emerald-600 text-white font-bold py-3 rounded-xl transition text-lg mb-3">▶️ اختبر الصوت قبل الحفظ</button>
              <p class="text-green-200/70 text-sm">✓ استمع للصوت قبل التأكيد</p>
            </div>

            <!-- التنبيهات -->
            <div class="bg-gradient-to-br from-purple-600/40 to-purple-600/20 backdrop-blur-md rounded-3xl p-8 border-2 border-purple-500/40 card-entrance" style="animation-delay: 0.2s">
              <div class="flex items-center gap-3 mb-6">
                <span class="text-4xl">🔊</span>
                <h2 class="text-2xl font-bold text-purple-300">التنبيهات</h2>
              </div>
              <div class="space-y-4">
                <label class="flex items-center gap-4 cursor-pointer">
                  <input type="checkbox" id="notification-prayer" checked class="w-6 h-6 rounded">
                  <span class="text-white text-lg">تنبيهات أوقات الصلاة</span>
                </label>
                <label class="flex items-center gap-4 cursor-pointer">
                  <input type="checkbox" id="notification-iftar" checked class="w-6 h-6 rounded">
                  <span class="text-white text-lg">تنبيهات الإفطار والسحور</span>
                </label>
                <label class="flex items-center gap-4 cursor-pointer">
                  <input type="checkbox" id="notification-quran" checked class="w-6 h-6 rounded">
                  <span class="text-white text-lg">تنبيهات ختم القرآن</span>
                </label>
              </div>
            </div>

            <!-- معلومات التطبيق -->
            <div class="bg-gradient-to-br from-yellow-600/40 to-yellow-600/20 backdrop-blur-md rounded-3xl p-8 border-2 border-yellow-500/40 card-entrance" style="animation-delay: 0.3s">
              <div class="flex items-center gap-3 mb-6">
                <span class="text-4xl">ℹ️</span>
                <h2 class="text-2xl font-bold text-yellow-300">معلومات التطبيق</h2>
              </div>
              <div class="space-y-3 text-yellow-200/80">
                <p><span class="font-bold">الإصدار:</span> 2.0.0</p>
                <p><span class="font-bold">آخر تحديث:</span> رمضان 1445 هـ</p>
                <p><span class="font-bold">الحجم:</span> خفيف وسريع</p>
                <p class="text-sm mt-4">تم تطويره بـ ❤️ لكل مسلم يريد قضاء رمضان الأفضل</p>
              </div>
            </div>

            <!-- زر الحفظ والإغلاق -->
            <div class="space-y-3 mt-8 sticky bottom-0 bg-gradient-to-t from-black to-transparent pt-4">
              <button onclick="saveSettingsFullPage()" class="w-full bg-gradient-to-r from-yellow-500/80 to-orange-500/80 hover:from-yellow-500 hover:to-orange-500 text-white font-bold py-4 rounded-xl transition text-lg transform hover:scale-105">✓ حفظ الإعدادات</button>
              <button onclick="this.closest('.fixed').remove(); playSound('soft');" class="w-full bg-gradient-to-r from-red-600/60 to-red-700/60 hover:from-red-600 hover:to-red-700 text-white font-bold py-3 rounded-xl transition text-lg">← إغلاق بدون حفظ</button>
            </div>
          </div>
        </div>
      `;
      document.body.appendChild(modal);
      
      document.getElementById('city-select-fullpage').value = currentLocation;
      document.getElementById('azan-sound-fullpage').value = localStorage.getItem('azan-sound') || 'default';
    }

    function saveSettingsFullPage() {
      const newLocation = document.getElementById('city-select-fullpage').value;
      const newSound = document.getElementById('azan-sound-fullpage').value;
      
      localStorage.setItem('azan-sound', newSound);
      
      if (newLocation !== currentLocation) {
        const [lat, lng] = newLocation.split(',').map(parseFloat);
        currentLocation = newLocation;
        fetchPrayerTimes(lat, lng);
      }
      
      playSound('soft');
      const modal = document.querySelector('.fixed.inset-0.bg-black');
      if (modal) modal.remove();
    }

    function switchSection(sectionId, element) {
      playSound('click');
      
      const modal = document.createElement('div');
      modal.className = 'fixed inset-0 bg-black/95 backdrop-blur z-50 overflow-y-auto';
      modal.innerHTML = `
        <div class="max-w-6xl mx-auto px-4 py-8">
          <button onclick="this.closest('.fixed').remove(); playSound('soft')" class="fixed top-4 right-4 bg-gradient-to-r from-red-600 to-red-700 hover:from-red-700 hover:to-red-800 text-white font-bold px-6 py-3 rounded-full text-3xl z-60 transition transform hover:scale-110 leading-none">←</button>
          <div id="full-section-content" class="mt-20"></div>
        </div>
      `;
      document.body.appendChild(modal);
      
      const contentContainer = modal.querySelector('#full-section-content');
      
      if (sectionId === 'prayer-section') {
        renderPrayerContent(contentContainer);
      } else if (sectionId === 'quran-section') {
        renderTasbihContent(contentContainer);
      } else if (sectionId === 'dua-section') {
        renderDuaContent(contentContainer);
      } else if (sectionId === 'tips-section') {
        renderTipsContent(contentContainer);
      }
    }

    function renderPrayerContent(container) {
      container.innerHTML = `
        <div class="space-y-6 card-entrance">
          <h2 class="text-3xl font-bold text-yellow-300 text-center">مواقيت الصلاة الكاملة</h2>
          <div id="detailed-prayer-times" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5"></div>
        </div>
      `;
      renderDetailedPrayerTimes();
    }

    function renderDetailedPrayerTimes() {
      const container = document.getElementById('detailed-prayer-times');
      prayers.forEach((prayer, index) => {
        const time = prayerTimes[prayer.id] || '--:--';
        const div = document.createElement('div');
        div.className = 'bg-gradient-to-br from-yellow-600/40 to-orange-600/30 backdrop-blur-md rounded-2xl p-6 border-2 border-yellow-500/40 card-entrance text-center hover:border-yellow-500/80 transition';
        div.style.animationDelay = `${index * 0.1}s`;
        div.innerHTML = `
          <p class="text-4xl mb-3">${prayer.icon}</p>
          <h3 class="text-yellow-300 font-bold text-lg mb-2">${prayer.name}</h3>
          <p class="amiri text-3xl text-white font-bold">${time}</p>
        `;
        container.appendChild(div);
      });
    }

    function renderDuaContent(container) {
      let html = '<div class="space-y-5 card-entrance"><h2 class="text-3xl font-bold text-purple-300 mb-6 text-center">🤲 أدعية رمضان المختارة</h2>';
      duas.forEach((dua, index) => {
        html += `
          <div class="bg-gradient-to-br from-purple-600/40 to-pink-600/30 backdrop-blur-md rounded-2xl p-6 border-2 border-purple-500/40 card-entrance hover:border-purple-500/80 transition cursor-pointer hover:bg-gradient-to-br hover:from-purple-600/50 hover:to-pink-600/40" style="animation-delay: ${index * 0.1}s" onclick="expandDua(${index})">
            <h3 class="text-purple-300 font-bold text-xl mb-3">${dua.title}</h3>
            <p class="amiri text-base text-white/80 leading-loose mb-2">${dua.content.substring(0, 100)}...</p>
            <p class="text-purple-200/60 text-sm">📌 ${dua.details}</p>
            <p class="text-purple-200 mt-3 text-sm">👆 اضغط لرؤية الدعاء كاملاً</p>
          </div>
        `;
      });
      html += '</div>';
      container.innerHTML = html;
    }

    function expandDua(index) {
      const dua = duas[index];
      const expanded = document.createElement('div');
      expanded.className = 'fixed inset-0 bg-black/95 backdrop-blur z-60 overflow-y-auto';
      expanded.innerHTML = `
        <div class="max-w-4xl mx-auto px-4 py-8">
          <button onclick="this.closest('.fixed').remove(); playSound('soft')" class="fixed top-4 right-4 bg-gradient-to-r from-red-600 to-red-700 hover:from-red-700 hover:to-red-800 text-white font-bold px-6 py-3 rounded-full text-3xl z-70 transition transform hover:scale-110 leading-none">←</button>
          <div class="mt-20">
            <div class="bg-gradient-to-br from-purple-600/40 to-pink-600/30 backdrop-blur-md rounded-3xl p-8 border-2 border-purple-500/40 card-entrance">
              <h2 class="text-4xl font-bold text-purple-300 mb-8 text-center">${dua.title}</h2>
              <div class="bg-black/40 rounded-2xl p-8 mb-6">
                <p class="amiri text-2xl text-center text-white leading-loose mb-6">${dua.content}</p>
                <p class="text-purple-300 text-center font-bold text-lg">📌 ${dua.details}</p>
              </div>
              <button onclick="this.closest('.fixed').remove(); playSound('soft')" class="w-full bg-purple-500/30 hover:bg-purple-500/60 border-2 border-purple-500/50 rounded-xl py-3 font-bold transition transform hover:scale-105">← العودة</button>
            </div>
          </div>
        </div>
      `;
      document.body.appendChild(expanded);
    }

    function renderTipsContent(container) {
      let html = '<div class="card-entrance"><h2 class="text-3xl font-bold text-blue-300 mb-8 text-center">💡 نصائح رمضانية مهمة</h2><div class="grid grid-cols-1 sm:grid-cols-2 gap-5">';
      tips.forEach((tip, index) => {
        html += `
          <div class="bg-gradient-to-br from-blue-600/40 to-cyan-600/30 backdrop-blur-md rounded-2xl p-6 border-2 border-blue-500/40 card-entrance hover:border-blue-500/80 transition" style="animation-delay: ${index * 0.1}s">
            <div class="flex items-center gap-3 mb-4">
              <span class="text-5xl">${tip.icon}</span>
              <h3 class="text-blue-300 font-bold text-lg">${tip.title}</h3>
            </div>
            <p class="text-blue-200/80 text-base">${tip.desc}</p>
          </div>
        `;
      });
      html += '</div></div>';
      container.innerHTML = html;
    }

    function renderTasbihContent(container) {
      let html = '<div class="card-entrance"><h2 class="text-4xl font-bold text-green-300 mb-8 text-center">🤲 قائمة التسبيح</h2>';
      html += '<div class="bg-gradient-to-br from-green-600/30 to-emerald-600/20 backdrop-blur-md rounded-3xl p-8 border-2 border-green-500/40">';
      html += '<p class="text-center text-green-200 mb-8 text-lg">اختر التسبيح الذي تريد وابدأ العد</p>';
      html += '<div class="space-y-4">';
      
      tasbeehs.forEach((tasbeeh, idx) => {
        html += `
          <div class="bg-black/30 rounded-2xl p-6 cursor-pointer hover:bg-black/50 transition border-2 border-green-500/30 hover:border-green-500/70" onclick="startTasbeeh(${idx})">
            <div class="flex items-center justify-between mb-3">
              <div class="flex items-center gap-3">
                <span class="text-4xl">${tasbeeh.icon}</span>
                <h3 class="text-green-300 font-bold text-xl">${tasbeeh.text}</h3>
              </div>
              <span class="text-green-200 text-lg">عدد: ${tasbeeh.count}</span>
            </div>
            <p class="text-green-200/60">👆 اضغط لبدء التسبيح</p>
          </div>
        `;
      });
      
      html += '</div></div></div>';
      container.innerHTML = html;
    }

    function startTasbeeh(index) {
      const tasbeeh = tasbeehs[index];
      let count = 0;
      
      const tasbeehModal = document.createElement('div');
      tasbeehModal.className = 'fixed inset-0 bg-black/95 backdrop-blur z-60 flex items-center justify-center';
      tasbeehModal.innerHTML = `
        <div class="text-center">
          <div class="mb-8">
            <p class="text-green-200 text-2xl mb-4">${tasbeeh.icon}</p>
            <h2 class="text-4xl font-bold text-green-300 mb-4">${tasbeeh.text}</h2>
            <div class="bg-gradient-to-br from-green-600/40 to-emerald-600/30 rounded-full w-48 h-48 mx-auto flex items-center justify-center border-4 border-green-500/60">
              <span id="tasbeeh-count" class="text-7xl font-bold text-green-300">0</span>
            </div>
            <p class="text-green-200/60 mt-4">المتبقي: <span id="tasbeeh-remaining" class="text-green-300 font-bold">${tasbeeh.count}</span></p>
          </div>
          <div class="space-y-3">
            <button onclick="incrementTasbeeh()" class="w-64 bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white font-bold py-6 rounded-xl text-2xl transition transform hover:scale-105 transition-all" style="cursor: pointer;">
              ✅ تسبيح
            </button>
            <button onclick="resetTasbeeh()" class="block mx-auto bg-blue-500/30 hover:bg-blue-500/60 border-2 border-blue-500/50 rounded-xl px-6 py-2 font-bold transition">
              🔄 إعادة تعيين
            </button>
            <button onclick="this.closest('.fixed').remove(); playSound('soft')" class="block mx-auto bg-red-500/30 hover:bg-red-500/60 border-2 border-red-500/50 rounded-xl px-6 py-2 font-bold transition">
              ← إغلاق
            </button>
          </div>
        </div>
      `;
      document.body.appendChild(tasbeehModal);
      
      window.incrementTasbeeh = function() {
        if (count < tasbeeh.count) {
          count++;
          playSound('click');
          document.getElementById('tasbeeh-count').textContent = count;
          document.getElementById('tasbeeh-remaining').textContent = tasbeeh.count - count;
          
          if (count === tasbeeh.count) {
            playSound('soft');
            setTimeout(() => {
              const alertModal = document.createElement('div');
              alertModal.className = 'fixed inset-0 bg-black/95 backdrop-blur z-70 flex items-center justify-center';
              alertModal.innerHTML = `
                <div class="text-center bg-gradient-to-br from-green-600/40 to-emerald-600/30 rounded-3xl p-8 border-2 border-green-500/40">
                  <p class="text-6xl mb-4">🎉</p>
                  <h3 class="text-3xl font-bold text-green-300 mb-4">تم إكمال التسبيح!</h3>
                  <p class="text-green-200 mb-6">ألف مبروك عليك يا غالي!</p>
                  <button onclick="this.closest('.fixed').remove();" class="bg-green-600 hover:bg-green-700 text-white font-bold px-6 py-3 rounded-xl transition">✓ استمرار</button>
                </div>
              `;
              document.body.appendChild(alertModal);
            }, 300);
          }
        }
      };
      
      window.resetTasbeeh = function() {
        count = 0;
        document.getElementById('tasbeeh-count').textContent = '0';
        document.getElementById('tasbeeh-remaining').textContent = tasbeeh.count;
      };
    }

    // Sound effects
    function playSound(type) {
      const audioContext = new (window.AudioContext || window.webkitAudioContext)();
      const now = audioContext.currentTime;
      
      if (type === 'soft') {
        const osc = audioContext.createOscillator();
        const gain = audioContext.createGain();
        osc.connect(gain);
        gain.connect(audioContext.destination);
        osc.frequency.value = 400;
        gain.gain.setValueAtTime(0.1, now);
        gain.gain.exponentialRampToValueAtTime(0.01, now + 0.2);
        osc.start(now);
        osc.stop(now + 0.2);
      } else if (type === 'click') {
        const osc = audioContext.createOscillator();
        const gain = audioContext.createGain();
        osc.connect(gain);
        gain.connect(audioContext.destination);
        osc.frequency.setValueAtTime(800, now);
        osc.frequency.exponentialRampToValueAtTime(400, now + 0.1);
        gain.gain.setValueAtTime(0.15, now);
        gain.gain.exponentialRampToValueAtTime(0.01, now + 0.1);
        osc.start(now);
        osc.stop(now + 0.1);
      }
    }

    function playAzanSound(type) {
      const audioContext = new (window.AudioContext || window.webkitAudioContext)();
      const now = audioContext.currentTime;
      
      if (type === 'default') {
        const notes = [400, 500, 400, 600, 500, 400, 500, 600];
        notes.forEach((freq, i) => {
          const osc = audioContext.createOscillator();
          const gain = audioContext.createGain();
          osc.connect(gain);
          gain.connect(audioContext.destination);
          osc.frequency.value = freq;
          gain.gain.setValueAtTime(0.1, now + i * 0.3);
          gain.gain.exponentialRampToValueAtTime(0.01, now + i * 0.3 + 0.25);
          osc.start(now + i * 0.3);
          osc.stop(now + i * 0.3 + 0.25);
        });
      } else if (type === 'peaceful') {
        const osc = audioContext.createOscillator();
        const gain = audioContext.createGain();
        osc.connect(gain);
        gain.connect(audioContext.destination);
        osc.frequency.value = 432;
        gain.gain.setValueAtTime(0.1, now);
        gain.gain.exponentialRampToValueAtTime(0.01, now + 1);
        osc.start(now);
        osc.stop(now + 1);
      }
      
      playSound('soft');
    }

    function testAzanSound() {
      const soundType = document.getElementById('azan-sound-fullpage').value;
      playAzanSound(soundType);
    }

    // City select
    document.addEventListener('DOMContentLoaded', () => {
      const citySelect = document.getElementById('city-select');
      if (citySelect) {
        citySelect.addEventListener('change', (e) => {
          if (e.target.value) {
            currentLocation = e.target.value;
            playSound('click');
            const [lat, lng] = currentLocation.split(',');
            fetchPrayerTimes(parseFloat(lat), parseFloat(lng));
          }
        });
      }
    });

    function convertTo12Hour(time24) {
      const [hours, minutes] = time24.split(':').map(Number);
      const ampm = hours >= 12 ? 'م' : 'ص';
      const hours12 = hours % 12 || 12;
      return `${hours12}:${minutes.toString().padStart(2, '0')} ${ampm}`;
    }

    async function fetchPrayerTimes(lat, lng) {
      try {
        const today = new Date();
        const response = await fetch(
          `https://api.aladhan.com/v1/timings/${today.getDate()}-${today.getMonth() + 1}-${today.getFullYear()}?latitude=${lat}&longitude=${lng}&method=4&school=1`
        );
        const data = await response.json();
        
        if (data.code === 200) {
          const timings = data.data.timings;
          prayerTimes = {
            fajr: convertTo12Hour(timings.Fajr),
            sunrise: convertTo12Hour(timings.Sunrise),
            dhuhr: convertTo12Hour(timings.Dhuhr),
            asr: convertTo12Hour(timings.Asr),
            maghrib: convertTo12Hour(timings.Maghrib),
            isha: convertTo12Hour(timings.Isha)
          };
          
          renderCurrentPrayerTimes();
          updateNextPrayer();
          document.getElementById('iftar-time').textContent = prayerTimes.maghrib;
          document.getElementById('suhoor-time').textContent = prayerTimes.fajr;
        }
      } catch (error) {
        console.error('Error fetching prayer times:', error);
      }
    }

    function renderCurrentPrayerTimes() {
      const grid = document.getElementById('prayer-times-grid');
      grid.innerHTML = '';
      
      prayers.forEach((prayer, index) => {
        const time = prayerTimes[prayer.id] || '--:--';
        const card = document.createElement('div');
        card.className = `bg-gradient-to-br from-white/10 to-white/5 backdrop-blur-sm rounded-xl p-4 border-2 border-yellow-500/30 text-center card-entrance hover:border-yellow-500/70 transition transform hover:scale-110`;
        card.style.animationDelay = `${index * 0.1}s`;
        card.innerHTML = `
          <span class="text-4xl block mb-2">${prayer.icon}</span>
          <p class="text-yellow-200/80 text-sm font-semibold mb-1">${prayer.name}</p>
          <p class="amiri text-xl font-bold text-yellow-300">${time}</p>
        `;
        grid.appendChild(card);
      });
    }

    function updateNextPrayer() {
      const now = new Date();
      const currentHours = now.getHours();
      const currentMinutes = now.getMinutes();
      const currentTimeIn24 = currentHours * 60 + currentMinutes;
      
      let nextPrayer = null;
      let nextPrayerTime = null;
      let minDiff = Infinity;
      
      const prayerOrder = ['fajr', 'sunrise', 'dhuhr', 'asr', 'maghrib', 'isha'];
      
      for (const prayerId of prayerOrder) {
        const time12 = prayerTimes[prayerId];
        if (!time12) continue;
        
        const [timePart, ampm] = time12.split(' ');
        let [hours, minutes] = timePart.split(':').map(Number);
        
        if (ampm === 'م' && hours !== 12) hours += 12;
        if (ampm === 'ص' && hours === 12) hours = 0;
        
        const prayerMinutes = hours * 60 + minutes;
        
        let diff = prayerMinutes - currentTimeIn24;
        if (diff < 0) diff += 24 * 60;
        
        if (diff < minDiff && diff > 0) {
          minDiff = diff;
          nextPrayer = prayers.find(p => p.id === prayerId);
          nextPrayerTime = time12;
        }
      }
      
      if (nextPrayer) {
        document.getElementById('next-prayer-name').textContent = nextPrayer.name;
        document.getElementById('next-prayer-time').textContent = nextPrayerTime;
        startCountdown(nextPrayerTime);
      }
    }

    let countdownInterval;
    
    function startCountdown(targetTime) {
      if (countdownInterval) clearInterval(countdownInterval);
      
      const [targetHours, targetMinutes] = targetTime.split(':').map(Number);
      
      function updateCountdown() {
        const now = new Date();
        let target = new Date();
        target.setHours(targetHours, targetMinutes, 0, 0);
        
        if (target <= now) {
          target.setDate(target.getDate() + 1);
        }
        
        const diff = target - now;
        const hours = Math.floor(diff / (1000 * 60 * 60));
        const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
        const seconds = Math.floor((diff % (1000 * 60)) / 1000);
        
        document.getElementById('countdown-hours').textContent = hours.toString().padStart(2, '0');
        document.getElementById('countdown-minutes').textContent = minutes.toString().padStart(2, '0');
        document.getElementById('countdown-seconds').textContent = seconds.toString().padStart(2, '0');
        
        if (diff <= 0) {
          playSound('soft');
          updateNextPrayer();
        }
      }
      
      updateCountdown();
      countdownInterval = setInterval(updateCountdown, 1000);
    }

    // Initialize
    createStarfield();
    
    function detectUserLocation() {
      if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(
          (position) => {
            const lat = position.coords.latitude;
            const lng = position.coords.longitude;
            userLocation = { lat, lng };
            currentLocation = `${lat},${lng}`;
            fetchPrayerTimes(lat, lng);
          },
          (error) => {
            fetchPrayerTimes(21.4225, 39.8262);
          }
        );
      } else {
        fetchPrayerTimes(21.4225, 39.8262);
      }
    }
    
    detectUserLocation();
    setInterval(() => fetchPrayerTimes(...currentLocation.split(',').map(parseFloat)), 60 * 60 * 1000);
  </script>
 <script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'9d3258a7d37b5daa',t:'MTc3MTk3MTU4NS4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>